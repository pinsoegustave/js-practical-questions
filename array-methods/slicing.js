const colors = ['red', 'blue', 'green', 'yellow'];

const remove = colors.slice(1, 4);
// console.log(remove);

// console.log(colors);

const newColors = ['Ferrari', 'Lambos', 'BMW', 'Audi', 'Mercedes'];
const above = newColors.slice(2, 6);
console.log(above);
console.log(newColors);