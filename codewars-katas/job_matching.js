function match(candidate, job) {
        // if (candidate.minSalary < candidate.job1) {

        // }        
        console.log(candidate.minSalary);
        console.log(job.job2);
}
let candidate1 = 
    { minSalary: 120000 },
    job1 = { maxSalary: 130000 },
    job2 = { maxSalary: 80000 };


console.log(match(candidate1, job1))     // true