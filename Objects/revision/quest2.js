// 2. **Object.values():**
// - Using the same object from the previous question, write a program to return an array of all the values in the object.

const animal = {
    species: "dog",
    color: "brown",
    age: 5
};

console.log(Object.values(animal));